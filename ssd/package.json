{
  "name": "doe-ssd",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:prod": "esbuild src/index.tsx --bundle --minify --outfile=dist/index.js",
    "build:dev": "esbuild src/index.tsx --bundle --outfile=dist/index.js",
    "start": "esbuild src/index.tsx --bundle --watch --outdir=dist --serve=3000 --servedir=dist",
    "deploy": "npm run build:prod && firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@webgpu/types": "^0.1.32",
    "esbuild": "0.17.18",
    "firebase": "^9.21.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
